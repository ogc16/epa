name: epa
description: EPA - Multi-vendor on-demand delivery platform (Customer, Rider, Vendor)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.2.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  google_fonts: ^8.1.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  cached_network_image: ^3.3.1
  flutter_rating_bar: ^4.0.1
  image_picker: ^1.0.7
  carousel_slider: ^5.1.2
  badges: ^3.1.2
  shimmer: ^3.0.0
  google_sign_in: ^7.2.0
  flutter_svg: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
